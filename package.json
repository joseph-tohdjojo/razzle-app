{
  "name": "my-razzle-app",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf build",
    "start": "npm run clean && razzle start",
    "build": "npm run clean && razzle build",
    "test": "npm run clean && razzle test --env=jsdom",
    "start:prod": "NODE_ENV=production node build/server.js"
  },
  "dependencies": {
    "express": "4.17.3",
    "razzle": "3.0.0",
    "react": "16.12.0",
    "react-dom": "16.12.0",
    "react-router-dom": "5.1.2"
  },
  "devDependencies": {
    "@babel/register": "^7.7.4",
    "rimraf": "^3.0.0"
  }
}
